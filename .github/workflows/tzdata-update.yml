name: tzdata-update

on:
  schedule:
    # FIXME:
    - cron: '34 * * * *'

jobs:
  tzdata-update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Check and update
        run: |
          git clone --depth 1 https://github.com/unicode-org/icu-data.git -b master /tmp/icu-checkout
          echo a1
          ls /tmp/icu-checkout/icu-data/tzdata/icunew/
          echo a2
          ls /tmp/icu-checkout/icu-data/tzdata/icunew/ -r1a |head -1
          VERSION=`ls /tmp/icu-checkout/icu-data/tzdata/icunew/ -r1a |head -1`
          echo a3
          echo $VERSION
          if [ "$VERSION" == "`cat tzdata/version.txt`" ]
          then
            exit
          fi

          echo $VERSION > tzdata/version.txt
          tzdata/update.sh

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Updata tzdata.
          title: Updata tzdata.
